# Customizing the profile

1. Edit `profile.config.json`.
2. Run `npm run generate`.
3. Run `npm run check`.
4. Commit the regenerated README and assets.

The scheduled workflow derives the username from the repository owner, so the
profile becomes owner-aware automatically after the first Actions run.
